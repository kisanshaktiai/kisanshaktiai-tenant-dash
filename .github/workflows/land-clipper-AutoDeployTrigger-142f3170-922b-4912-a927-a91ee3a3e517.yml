name: Trigger auto deployment for land-clipper

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/land-clipper-AutoDeployTrigger-142f3170-922b-4912-a927-a91ee3a3e517.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.LANDCLIPPER_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.LANDCLIPPER_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.LANDCLIPPER_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.LANDCLIPPER_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.LANDCLIPPER_REGISTRY_PASSWORD }}
          containerAppName: land-clipper
          resourceGroup: kisanshakti-rg
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


